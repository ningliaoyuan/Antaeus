CREATE TYPE [dbo].[ArticleId]
	FROM varchar(11) NOT NULL
GO
CREATE TYPE [dbo].[ArticleTitle]
	FROM varchar(11) NOT NULL
GO
CREATE TYPE [dbo].[ArticleCategory]
	FROM varchar(11) NOT NULL
GO
CREATE TYPE [dbo].[ArticleValue]
	FROM varchar(11) NOT NULL
GO
CREATE TYPE [dbo].[ArticleView]
	FROM varchar(11) NOT NULL
GO